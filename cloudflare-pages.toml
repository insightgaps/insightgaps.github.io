# Build configuration for Cloudflare Pages (committed at repo root).
# Production = main branch. Every push builds via the single command below.

pythonVersion = "3.11"

[build]
command = "python scripts/build.py && python scripts/validate.py"
publish = "public"

[build.environment]
PYTHON_VERSION = "3.11"
